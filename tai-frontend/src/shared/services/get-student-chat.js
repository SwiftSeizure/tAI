import api from "./axios"; 

export const getStudentChat = async (id) => {
    // try {
    //     const response = await api.get(`/student-chat/${id}`);
    //     return response.data;
    // } catch (error) {
    //     console.error("Error fetching student chat:", error);
    //     throw error;
    // }
};
